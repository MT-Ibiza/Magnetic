import LoginForm from '@/app/components/login'
import React from 'react'

interface Props {}

function Page(props: Props) {
  const {} = props

  return (
    <LoginForm />
  )
}

export default Page
